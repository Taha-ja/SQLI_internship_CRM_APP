export interface RegResp {
    Email:string;
    ExpiresOn?:string;
    IsAuthenticated:boolean;
    Token:string;
    // Email:string,
    // IsConfirmed:boolean,
    // EmailSent:boolean,
    // EmailVerified:boolean
}