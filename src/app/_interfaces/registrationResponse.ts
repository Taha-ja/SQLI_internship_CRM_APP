export interface RegResp {
    Email:string;
    ExpiresOn?:string;
    IsAuthenticated:boolean;
    Token:string;
    message:string;
    // Email:string,
    // IsConfirmed:boolean,
    // EmailSent:boolean,
    // EmailVerified:boolean
}